import React from "react";

const Wishlist = () => {
    return (
        <section>
            <h2>Wishlist</h2>
            <p>No wishlist items yet.</p>
        </section>
    );
};

export default Wishlist;
